## 运行模式
当前提示运行在非交互环境中。

### 非交互规则
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 当出现可选分支时，直接选择合理默认值，并在结果里记录假设。
- 跳过可选确认，直接执行。

## 任务
在所选项目中执行一次结构化的 UI Master 任务。

## 执行上下文
- 工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析后的仓库提示：`{{repositoryPath}}`{{/if}}
- UI 任务说明：`{{{uiMasterDescription}}}`
{{#if sceneKey}}- 请求场景 key：`{{sceneKey}}`{{/if}}

{{#if uiMasterCommandIds}}
### 已选命令
{{#each uiMasterCommandIds}}
- `{{this}}`
{{/each}}
{{/if}}

{{#if targetScopeMarkdown}}
### 目标范围

{{{targetScopeMarkdown}}}
{{else}}
本次没有提供前端拼接的目标范围文本。不要假定超出当前项目根目录的仓库范围。
{{/if}}

{{#if referenceVaults}}

### 补充引用 Vault
{{#each referenceVaults}}
- `{{name}}` (`{{id}}`) 位于 `{{physicalPath}}`{{#if type}} [{{type}}]{{/if}}
{{/each}}
{{/if}}

{{#if targetProjects}}

### 补充项目引用
{{#each targetProjects}}
- `{{name}}` 位于 `{{physicalPath}}`（权限：`{{accessType}}`）
{{/each}}
{{/if}}

## 要求
1. 始终在 `{{projectPath}}` 内工作。
2. 所有修改必须围绕当前 UI 任务说明展开。
3. 所有代码修改都必须限制在被标记为 `write` 的仓库内。
4. 被标记为 `read` 的仓库只能作为只读上下文。
5. 将 captain / support 视为执行参考，不要把它们当成装饰性输出。
6. 优先落到具体 UI 工作：信息层级、状态、文案、交互、可访问性与实现细节。
7. 不要依赖交互式确认。
8. 默认使用用户输入所用语言回复；若混合语言，以最后一个明确语言指令为准。
9. 若没有可写仓库，只进行只读分析，并给出可执行的后续修改建议。

## 输出格式
- 必须按以下顺序输出：
  1. `## 结论`
  2. `## 假设（如有）`
  3. `## 下一步`
- 当无假设时，在 `## 假设（如有）` 下写 `无`。
- 若做出假设，必须使用固定格式：`假设：<一句话>；原因：<一句话>；影响：<一句话>`。

## 建议执行流程
1. 检查所选目标仓库，并区分哪些是 `write`、哪些是 `read`。
2. 将已选命令集合映射到当前任务说明。
3. 直接在代码中实现所需 UI 变更。
4. 保持修改聚焦，避免无关重构。
5. 在最终响应里总结结果与后续约束。
