{
  "$schema": "https://tasks.hagicode.com/schemas/task-preset-plugin/commands.schema.json",
  "schemaVersion": "1.1",
  "selectionMode": "single",
  "groups": [
    {
      "id": "monospecs",
      "title": { "key": "commands.groups.monospecs" },
      "description": { "key": "commands.groups.monospecsDescription" }
    }
  ],
  "commands": [
    {
      "id": "initialize",
      "groupId": "monospecs",
      "label": "initialize",
      "description": { "key": "commands.items.initialize.description" },
      "preludeTemplate": "Initialize or inspect the target project's MonoSpecs configuration."
    },
    {
      "id": "add-repository",
      "groupId": "monospecs",
      "label": "add-repository",
      "description": { "key": "commands.items.addRepository.description" },
      "preludeTemplate": "Append repositories inferred from the supplied URL or URLs in input order."
    },
    {
      "id": "reorder-repositories",
      "groupId": "monospecs",
      "label": "reorder-repositories",
      "description": { "key": "commands.items.reorderRepositories.description" },
      "preludeTemplate": "Preview and, only after confirmation, reorder repositories by observable activity."
    }
  ]
}
