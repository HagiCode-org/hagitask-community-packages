## 运行模式
本提示词运行在非交互环境中。

### 非交互规则
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 需要做可选决策时，选择合理默认值并记录该假设。
- 跳过可选确认，直接继续。
- 不要传入会打开编辑器或浏览器的参数（`gh pr create` 必须带 `--title` 和 `--body`）。

## 任务
创作一个新的 HagiTask Community Task 包，并创建 Pull Request 将其贡献到上游 Community Packages 仓库。

## 创作说明
- Community Task 名称：`{{{communityTaskName}}}`
- 目标与用户价值：

{{{communityTaskObjective}}}

- 输入字段与约束：

{{{communityTaskInputs}}}

- 执行行为与质量要求：

{{{communityTaskQuality}}}

- 验收标准：

{{{communityTaskAcceptance}}}

{{#if communityTaskPublication}}
- 发布说明（分类、标签、适用场景）：

{{{communityTaskPublication}}}
{{else}}
- 未提供发布说明。请从目标中推导分类、标签和适用场景，并把这一点记为假设。
{{/if}}

## 仓库身份
只通过公开 GitHub 身份定位仓库，不要依赖当前机器的目录布局。

- 上游 Community Packages：`https://github.com/HagiCode-org/hagitask-community-packages`
- 公开 Schema 根地址（HTTP 直取，无需 clone）：`https://tasks.hagicode.com/schemas/`

先用 `gh repo view <OWNER/REPO> --json name,owner,defaultBranchRef,isFork` 确认上游，并在后续所有命令中使用返回的元数据，尤其是上游默认分支。在结果中记录解析出的 upstream URL、fork URL 和默认分支，使该流程可以在其他环境复现。

`hagitask` 仓库**不是**你需要的仓库。它的 Schema 已发布在上述地址，通过 HTTP 获取即可。

## 本地上下文（只读）
- 临时工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析到的仓库提示：`{{repositoryPath}}`{{/if}}

`{{projectPath}}` 只是用于 clone 和构建的临时空间。它**不是** Community Packages 仓库，也**不是**包根目录。除非它就是你在第 3 步创建的 checkout，否则不要在其中写入包文件。

{{#if targetScopeMarkdown}}
### 只读仓库上下文

{{{targetScopeMarkdown}}}
{{else}}
未选择仓库范围。只使用公开的上游仓库与公开 Schema 根地址开展工作。
{{/if}}

{{#if referenceVaults}}
### 补充 Vault 引用（只读）
{{#each referenceVaults}}
- `{{name}}`（`{{id}}`），路径 `{{physicalPath}}`{{#if type}} [{{type}}]{{/if}}
{{/each}}
{{/if}}

{{#if targetProjects}}
### 补充项目引用（只读）
{{#each targetProjects}}
- `{{name}}`，路径 `{{physicalPath}}`（访问权限：`{{accessType}}`）
{{/each}}
{{/if}}

所选仓库、Vault 和项目引用**只是上下文**。无论标签如何显示，它们全部只读。它们都不是写入目标，也都不是 Community 包的存放位置。

## 执行流程

### 1. 校验前置条件
- 运行 `gh --version` 与 `git --version`。
- 运行 `gh auth status`。若显示未登录，立即停止，输出命令原始结果并给出 `gh auth login` 作为修复建议。不要继续。
- 运行 `gh api user --jq .login` 获取 fork owner 的登录名。

### 2. 复用或创建 fork
- 用 `gh repo view <upstream> --json defaultBranchRef,nameWithOwner` 读取上游元数据。
- 用 `gh repo view <fork-owner>/<upstream-repo-name> --json nameWithOwner,parent` 检查是否已有 fork。
- 若该仓库存在且 `parent` 指向上游，直接复用，不要创建第二个 fork。
- 若不存在，运行 `gh repo fork <upstream> --clone=false`，并等待 `gh repo view` 能解析到新建的 fork。
- 若因权限或组织策略无法创建 fork，停止并报告阻塞原因。任何情况下都不得退化为直接推送上游。

### 3. 准备临时 checkout
- 通过 URL 把 fork clone 到全新的临时目录，例如 `git clone https://github.com/<fork-owner>/<repo>.git`。
- 添加 upstream remote 并同步默认分支，避免基于过期分支工作。
- 运行 `git submodule update --init --recursive`。这一步只是校验的前置条件：`npm run validate` 会按文件名在 `hagitask/schemas/task-preset-plugin/` 下离线解析每个 `$schema`，该目录缺失时所有文档都会校验失败。Schema 内容仍然从公开 URL 读取，不从这里读。
- 运行 `git status --porcelain` 确认工作树干净。若存在预期之外的本地改动，停止并报告，不要覆盖用户内容。
- 创建工作分支，例如 `git switch -c add-community-task/<task-id>`。不要在 fork 的默认分支上提交。

### 4. 确定 canonical task id
- 从 Community Task 名称推导小写 kebab-case id，需匹配 `^[a-z0-9][a-z0-9-]*$`。
- 确认上游不存在同名 `data/<task-id>/`。若已存在，换一个不冲突的 id 并把该选择记为假设。目录名就是 canonical `taskId`，后续不可重命名。

### 5. 先阅读参考资料再动手
- 阅读 Community 仓库 `README.md`，掌握包布局、`$schema` 对照表和贡献规则。
- 阅读 `data/` 下结构最接近的既有包作为范例。
- 从公开地址拉取即将使用的每一份权威 Schema，例如 `https://tasks.hagicode.com/schemas/task-preset-plugin/manifest.schema.json`。文件名与第 6 步的对照表一致，`https://tasks.hagicode.com/schemas/` 是它们共同的根地址。

### 6. 在 `data/<task-id>/` 下编写包
创建全部文件。缺少其中任何一项都视为包不完整：

| 文件 | 要求 |
| --- | --- |
| `manifest.json` | `taskPresetId` 等于目录名，`version` 为 `1.0.0` 语义化版本，含 `status`、`kind`、`owner`、`localization`（`en-US` + `zh-CN` bundles）、`ui.panel`、`site.storePage`、`backend.taskPreset`、`backend.prompts` |
| `frontend/panel.json` | 只使用现有 renderer 的面板定义 |
| `frontend/commands.json` | 仅在任务确实提供命令目录时创建；否则同时省略该文件与 `ui.commands` |
| `backend/task-preset.json` | `taskKey`、`scriptKey`、`defaultTargetType`、三类 `targets` 集合、`requirements`、`inputBindings` |
| `backend/prompts.json` | inputs、`handlebars` 引擎，以及 `en-US` + `zh-CN` 的模板对 |
| `backend/templates/en-US/system.md`、`user.hbs` | 英文执行规则 |
| `backend/templates/zh-CN/system.md`、`user.hbs` | 中文模板，约束覆盖面与英文一致 |
| `locales/en-US.json`、`locales/zh-CN.json` | key 集合完全一致，仅自然语言不同 |
| `store-page/index.en-US.md`、`index.zh-CN.md` | frontmatter 含 `locale`、`slug`、`title`、`summary`、`status`，并在 `en-US` 页补充 `catalog` 与 `tags` |

上表中每个 JSON 文档都要带上公开 Schema 根地址下的绝对 `$schema` URL。顶层文件与嵌套文件使用同一段基础地址，仅文件名不同，取值必须与下表完全一致：

| 包内文件 | `$schema` |
| --- | --- |
| `manifest.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/manifest.schema.json` |
| `frontend/panel.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/panel.schema.json` |
| `frontend/commands.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/commands.schema.json` |
| `backend/task-preset.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/task-preset.schema.json` |
| `backend/prompts.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/prompt-package.schema.json` |
| `locales/<locale>.json` | `https://tasks.hagicode.com/schemas/task-preset-plugin/locales.schema.json` |

编写时的硬性规则：

- 绝不能把 `raw.githubusercontent.com` 地址、`github.com` blob 地址或相对路径写进 `$schema`。公开 URL 是唯一被接受的形式；校验器按其 `schemas/task-preset-plugin/` 路径尾部匹配，因此公开 URL 与嵌套子模块解析到同一份权威 Schema。
- manifest 声明的每个路径都必须真实存在，且必须位于包目录之内。
- `store-page` 的 `slug` 必须等于目录名，每个文件的 `locale` 必须与文件名中的 locale 一致。
- `catalog` 条目必须是小写 kebab-case；自由文本或本地化文案放进 `tags` 或 `badges`。
- frontmatter 保持扁平：只使用 `key: value` 行与 `  - item` 序列。嵌套映射、行内数组和多行标量都不会被解析。
- 面板字段的 `output`、`task-preset.json` 的 `inputBindings[].input` 与 `prompts.json` 的 input 名称必须一一对应。绑定找不到对应面板输出，或 prompt 参数没有对应绑定，都属于缺陷。
- 面板中每个 `{ "key": "..." }` 引用都必须在两个 locale bundle 中都能命中真实的 key。没有任何工具会校验这一点，必须自行检查。
- 让产出的提示模板保持非交互、明确写入范围、如实对待校验结果，并满足说明中的质量要求。

### 7. 真实执行校验
在 Community 仓库根目录执行：

```bash
npm install
npm run validate
npm test
```

- 如实报告每条命令的真实退出状态。
- 若 `npm run validate` 失败，修改 `data/<task-id>/` 下的源文件后重新运行。不要修改生成产物，也不要屏蔽错误。
- 若仓库的 fixture 测试枚举了已知包列表，扩展该列表，不要删除断言。

### 8. 提交并创建 Pull Request
- 只暂存 `data/<task-id>/` 下的文件，以及仓库要求扩展的 fixture 列表。用 `git status --porcelain` 确认没有暂存 `hagitask/` 下的任何内容。
- 使用描述性提交信息提交，并把分支推送到 fork。
- 向上游创建 Pull Request：

```bash
gh pr create \
  --repo <upstream-owner>/<upstream-repo> \
  --head <fork-owner>:<branch> \
  --base <upstream-default-branch> \
  --title "<title>" \
  --body "<body>"
```

- 用 `gh pr view <number> --repo <upstream> --json headRepositoryOwner,headRefName,baseRefName,url` 校验 head 与 base。base 必须是上游 Community Packages 仓库，不能是 `hagitask` 仓库，也不能是 fork 自身。
- 若创建 PR 失败，输出准确的错误信息，并保留已推送的分支，便于用户手动完成。

## 要求
1. 一律通过公开 GitHub 身份定位仓库，不使用本地相对路径。
2. 只在 fork 工作分支的 `data/<task-id>/` 内写入。
3. 用户的仓库、Vault 和项目引用一律视为只读上下文。
4. 不修改 `hagitask/` 下的任何内容，也不为了读取 Schema 去 clone `hagitask` 仓库——统一从 `https://tasks.hagicode.com/schemas/` 获取。
5. 生成全部必需的包资源，不交付半成品。
6. 校验前先初始化嵌套子模块；校验器需要它来离线解析 `$schema`。
7. 运行真实的校验命令并如实报告结果。
8. 复用已有 fork，不重复创建。
9. 遇到登录、权限或 GitHub 操作失败时，停在带可执行诊断的阻塞状态，不得谎报成功。
10. 默认使用用户输入语言回复；混合语言输入时，遵循最后一次明确的语言指令。

## 人工审阅提醒
校验通过只能证明结构正确，不代表内容质量合格。它不检查 locale key 是否对齐、`{ "key": ... }` 引用是否命中、提示词文本是否通顺，也不检查 store page 描述是否与真实行为一致。把这些列为待审阅项，不要把绿色结果当作内容审核通过。

## 输出格式
- 严格按以下顺序输出：
  1. `## Conclusion`
  2. `## Contribution result`
  3. `## Assumptions (if any)`
  4. `## Next step`
- 在 `## Contribution result` 中必须报告：canonical task id、创建的文件、upstream URL、fork URL（以及是复用还是新建）、上游默认分支、工作分支、各校验命令的退出状态、人工审阅项，以及 PR 编号与 URL。若流程提前终止，用准确的阻塞步骤和命令输出替代 PR 信息。
- 没有假设时在假设小节写 `None`。
- 需要假设时使用该固定格式：`Assumption: <一句话>; Reason: <一句话>; Impact: <一句话>`。