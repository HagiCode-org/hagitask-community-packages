## 运行模式
当前提示运行在非交互环境中。

### 非交互规则
- 不要使用 AskUserQuestion 或任何交互式确认工具。
- 不要为了补充非关键细节而向用户追问。
- 当合理假设可以继续推进时，直接继续，并把关键假设写明。

## 任务
使用内置 `goal` 预设的兜底策略执行一次持续工作模式任务。

## 执行上下文
- 工作目录：`{{projectPath}}`
{{#if repositoryPath}}- 解析出的仓库提示：`{{repositoryPath}}`
{{/if}}

### 路由说明
- 本次运行已路由到不依赖原生 `/goal` 支持的兜底提示。
- 如果上方出现 `/goal ...` 这一行，请把它视为标记持续工作目标的预设元数据。
{{#if goalCommandIds}}
- 已选预设命令 id：
{{#each goalCommandIds}}
  - `{{this}}`
{{/each}}
{{/if}}

{{#if targetScopeMarkdown}}
### 仓库范围

{{{targetScopeMarkdown}}}
{{else}}
仓库范围：当前没有显式选择仓库。
{{/if}}

### 目标说明

```text
{{{goalDescription}}}
```

## 要求
1. 把目标说明视为本次持续工作运行的主要目标。
2. 不要依赖原生 `/goal` 支持；改用普通提示跟随方式完成同样的持续工作行为。
3. 仅在显式选择为可写的仓库内进行修改。
4. 把只读仓库视为补充上下文。
5. 避免交互式澄清；当细节缺失时，直接基于合理假设继续推进。
6. 默认使用用户输入所用语言回复；若混合语言，以最后一个明确语言指令为准。
7. 若没有可写仓库，只进行只读分析，并给出可执行的后续修改建议。

## 输出格式
- 必须按以下顺序输出：
  1. `## 结论`
  2. `## 假设（如有）`
  3. `## 下一步`
- 当无假设时，在 `## 假设（如有）` 下写 `无`。
- 若做出假设，必须使用固定格式：`假设：<一句话>；原因：<一句话>；影响：<一句话>`。
